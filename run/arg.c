#include <stdio.h>

/**
 * main -
 * @ac: argument count
 * @av: argument vector
 * Return: 0
 */
int main(int ac, char **av)
{
	ac (void);

	while (av != NULL)
	{
		printf("%s\n", *av);
	}
	return (0);
}
